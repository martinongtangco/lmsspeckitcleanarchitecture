@using LibreLms.Host.Pages.Courses
@model EnrollmentResult

@if (Model.Success)
{
    <div>
        <span class="tag tag-accent-2">✓ Enrolled</span>
        @if (Model.IsScorm == true)
        {
            <a asp-page="/Scorm/Launch" asp-route-courseId="@Model.CourseId" class="btn btn-primary">Launch SCORM Course</a>
        }
    </div>
}
else if (Model.MessageType == "warning")
{
    <div>
        <span class="text-muted" style="font-weight: 500;">@Model.Message</span>
    </div>
}
else
{
    <div>
        <div class="alert alert-danger mb-md">@Model.Message</div>
        <form hx-post="/Courses/Detail/@Model.CourseId?handler=Enroll"
              hx-swap="outerHTML"
              hx-target="#enroll-region">
            <button type="submit" class="btn btn-primary">Retry</button>
        </form>
    </div>
}
